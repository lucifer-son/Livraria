package repositorio;

import model.WishList;

/**
 * Interface específica para o repositório de WishLists.
 */
public interface WishListRepositorio extends RepositorioBase<WishList, String> {
    // Nenhum método específico por enquanto.
}
