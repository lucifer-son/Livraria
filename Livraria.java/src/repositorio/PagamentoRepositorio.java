package repositorio;

import model.Pagamento;

/**
 * Interface específica para o repositório de Pagamentos.
 */
public interface PagamentoRepositorio extends RepositorioBase<Pagamento, String> {
    // Nenhum método específico por enquanto.
}
